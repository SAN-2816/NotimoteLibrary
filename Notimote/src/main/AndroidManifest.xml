<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devsan.notimote">

    <application>
        <activity android:name=".locktimote.LocktimoteActivity"></activity>
        <activity android:name=".lockScreen.LockScreenActivity" />

        <receiver android:name=".lockScreen.LockReceiver" />

        <service android:name=".lockScreen.LockService" />
    </application>

</manifest>